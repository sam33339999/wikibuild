# Local environment / secrets — never commit
.env

# Build artifacts (anchored to repo root so cmd/wikibuild/ source is tracked)
/wikibuild
/wikibuild.exe
cover.out

# Runtime upload content
/content/

# Editor / OS
.DS_Store
*.swp
